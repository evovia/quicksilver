<nav class="w-52 h-full overflow-auto">
  <div
    class="
      px-4 py-2 border-r border-gray-200 space-y-4 flex flex-col
      justify-between h-full
    "
  >
    <div class="space-y-4">
      <%= render Sidebar::Category.new(
        heading: "Getting started",
        items: Content::Page.all.reject(&:root?).sort_by(&:position)
      ) %>

      <%= render Sidebar::Category.new(
        heading: "Affordances",
        items: Content::Affordance.all
      ) %>

      <%= render Sidebar::Category.new(
        heading: "Components",
        items: Content::Component.all,
        text_method: :name
      ) %>
    </div>

    <div>
      <%= link_to "https://evovia.com" do %>
        <%= image_tag "logo_green.svg", alt: "Evovia logo" %>
      <% end %>
    </div>
  </div>
</nav>
